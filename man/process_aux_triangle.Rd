% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_engineering.R
\name{process_aux_triangle}
\alias{process_aux_triangle}
\title{Process an auxiliary reporting triangle into prefixed feature columns}
\usage{
process_aux_triangle(
  df,
  name,
  lagged_term_list,
  temporal_resol,
  smoothed,
  max_report_override = NULL
)
}
\arguments{
\item{df}{Data frame with columns \code{reference_date}, \code{report_date}, \code{lag},
and \code{value} (the signal values).}

\item{name}{Character scalar used as the column prefix (e.g. \code{"nssp"}).}

\item{lagged_term_list}{Numeric vector of lag values (same as used for the
primary signal).}

\item{temporal_resol}{\code{"daily"} or \code{"weekly"}.}

\item{smoothed}{Logical; if \code{FALSE} and \code{temporal_resol == "daily"}, a 7-day
moving average is computed. Otherwise \code{value_7dav} is set equal to
\code{value_raw}.}
}
\value{
Data frame with columns \code{reference_date}, \code{report_date}, \code{lag}, and
all value/log columns prefixed with \verb{\{name\}_}.  Useful predictors are
\verb{\{name\}_log_value_7dav_lag\{N\}} and \verb{\{name\}_log_delta_value_7dav_lag\{N\}};
see \code{\link[=aux_feature_names]{aux_feature_names()}}.
}
\description{
Runs a single auxiliary data frame (one geo, one signal) through the
fill → 7-day-average → lagged-terms → log-transform pipeline and
renames all value-derived columns with a \verb{\{name\}_} prefix so they can
be safely joined to the primary preprocessed data frame.
}
